/**
 * ConnectivityBanner — Legacy component, now a no-op.
 *
 * Connectivity status is displayed in the AppLayout sticky header.
 * This component is kept to avoid breaking existing imports
 * while the doctor pages are migrated to AppLayout.
 */
export default function ConnectivityBanner() {
  return null;
}
